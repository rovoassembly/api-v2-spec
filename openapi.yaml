openapi: 3.1.0
info:
  title: Assembly Studio API
  version: "2"
  description: >
    Customer-facing API for the Assembly Studio garment ordering and configuration platform.
    Backoffice, admin, system, and ERP endpoints are out of scope for this version.
  license:
    name: Proprietary

servers:
  - url: /api/v2
    description: TBD

security: []

tags:
  - name: Catalog
    description: Base products and dye options available for configuration.
  - name: Configuration
    description: Create and manage garment configurations.
  - name: Calculator
    description: Price and lead-time estimation.
  - name: Upload
    description: File upload and conversion.
  - name: Cart
    description: Cart management, checkout, and order placement.
  - name: Commerce
    description: VAT validation and other commerce utilities.
  - name: Translations
    description: UI translation string bundles, sourced from Storyblok.
  - name: Countries
    description: Supported countries for billing and shipping address forms.

paths:

  # ─── Catalog ──────────────────────────────────────────────────────────────

  /base-products:
    get:
      tags: [Catalog]
      summary: List all base products
      description: >
        Returns all visible base products, pre-sorted server-side.
        Cache: 60 minutes. `locale` is part of the cache key.
      parameters:
        - $ref: '#/components/parameters/Locale'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BaseProduct'

  /base-products/{slugOrId}:
    get:
      tags: [Catalog]
      summary: Get a base product by slug or UUID
      description: >
        The server detects whether the value is a slug or UUID automatically.
        Cache: 60 minutes. `locale` is part of the cache key.
      parameters:
        - name: slugOrId
          in: path
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/Locale'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BaseProduct'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /base-products/{slugOrId}/woven-label-options:
    get:
      tags: [Catalog]
      summary: Get woven label options for a base product
      description: >
        Returns only options relevant to the product — neck or cap, resolved server-side.
        Cache: 60 minutes. `locale` is part of the cache key.
      parameters:
        - name: slugOrId
          in: path
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/Locale'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WovenLabelOptions'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /dyes:
    get:
      tags: [Catalog]
      summary: List all dye categories with their colors
      description: >
        Returns all dye color categories, each containing their colors. Pre-sorted server-side.
        Cache: 60 minutes. `locale` is part of the cache key.
      parameters:
        - $ref: '#/components/parameters/Locale'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DyeCategory'

  # ─── Configuration ────────────────────────────────────────────────────────

  /configuration:
    post:
      tags: [Configuration]
      summary: Create a configuration
      parameters:
        - $ref: '#/components/parameters/Locale'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [baseProductId]
              properties:
                baseProductId:
                  type: string
                deliverySpeed:
                  type: string
                  enum: [flexible, fast]
                  default: flexible
      responses:
        '200':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Configuration'
        '400':
          description: Validation failure
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'

  /configuration/{id}:
    get:
      tags: [Configuration]
      summary: Get a configuration
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/Locale'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Configuration'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags: [Configuration]
      summary: Update a configuration
      description: >
        Partially updates a configuration, including any of its customizations.
        The backend resolves which customization record to create or modify.
        Only send fields that need updating.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/Locale'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfigurationUpdate'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Configuration'
        '400':
          description: Validation failure
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  # ─── Calculator ───────────────────────────────────────────────────────────

  /calculator:
    get:
      tags: [Calculator]
      summary: Calculate pricing and lead time
      description: >
        Computes pricing, lead time, and development costs for a product configuration.
        Does not require a saved configuration — computes on the fly.
        Query params support nested objects (parsed via `qs`, use deepObject style).
      parameters:
        - $ref: '#/components/parameters/Locale'
        - name: baseProductId
          in: query
          required: true
          schema:
            type: string
        - name: color
          in: query
          required: true
          style: deepObject
          explode: true
          schema:
            type: object
            required: [technique, category]
            properties:
              technique:
                type: string
                description: Dye technique ID
              category:
                type: string
                description: Dye color category ID
              dye:
                type: string
                description: Specific dye color ID — defaults to category default if omitted
        - name: quantity
          in: query
          required: true
          schema:
            type: integer
            minimum: 1
        - name: print
          in: query
          required: false
          style: deepObject
          explode: true
          schema:
            type: object
            properties:
              front:
                $ref: '#/components/schemas/CalculatorPrintSideInput'
              back:
                $ref: '#/components/schemas/CalculatorPrintSideInput'
              left:
                $ref: '#/components/schemas/CalculatorPrintSideInput'
              right:
                $ref: '#/components/schemas/CalculatorPrintSideInput'
        - name: wovenLabel
          in: query
          required: false
          style: deepObject
          explode: true
          schema:
            type: object
            properties:
              size:
                type: string
                description: Label size ID
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalculatorResponse'
        '400':
          description: Validation failure
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'

  # ─── Upload ───────────────────────────────────────────────────────────────

  /upload:
    post:
      tags: [Upload]
      summary: Upload and optionally convert a file
      description: >
        Uploads a file and optionally converts it to a raster format (e.g. .ai → .png).
        Returns a streaming NDJSON response — each newline-delimited JSON object is one event.
      parameters:
        - $ref: '#/components/parameters/Locale'
        - name: convert
          in: query
          required: false
          schema:
            type: boolean
          description: If true, the uploaded file will also be converted
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: "Streaming NDJSON upload events (progress: 0\u2013100)"
          content:
            application/x-ndjson:
              schema:
                $ref: '#/components/schemas/UploadEvent'

  # ─── Cart ─────────────────────────────────────────────────────────────────

  /cart/{cartId}:
    get:
      tags: [Cart]
      summary: Get a cart
      parameters:
        - name: cartId
          in: path
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/Locale'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cart'
        '404':
          description: Not found or invalid ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /cart/{cartId}/items:
    post:
      tags: [Cart]
      summary: Add a line item to the cart
      description: Pricing is resolved and locked server-side at this point.
      parameters:
        - name: cartId
          in: path
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/Locale'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [configurationId, quantity]
              properties:
                configurationId:
                  type: string
                quantity:
                  type: integer
                  minimum: 1
      responses:
        '200':
          description: Updated cart
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cart'

  /cart/{cartId}/items/{itemId}:
    put:
      tags: [Cart]
      summary: Update a line item's size allocations
      parameters:
        - name: cartId
          in: path
          required: true
          schema:
            type: string
        - name: itemId
          in: path
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/Locale'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sizeAllocations]
              properties:
                sizeAllocations:
                  type: array
                  items:
                    type: object
                    required: [size, quantity]
                    properties:
                      size:
                        type: string
                        example: L
                      quantity:
                        type: integer
                        minimum: 0
      responses:
        '200':
          description: Updated line item
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CartLineItem'
        '400':
          description: Validation failure
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
    delete:
      tags: [Cart]
      summary: Remove a line item from the cart
      parameters:
        - name: cartId
          in: path
          required: true
          schema:
            type: string
        - name: itemId
          in: path
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/Locale'
      responses:
        '200':
          description: Updated cart
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cart'

  /cart/{cartId}/addresses:
    patch:
      tags: [Cart]
      summary: Set billing and/or shipping addresses
      description: >
        Supports progressive saves — call as the user fills in the form so data
        is not lost on page refresh. Full validation only happens at checkout.
      parameters:
        - name: cartId
          in: path
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/Locale'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                billingAddress:
                  $ref: '#/components/schemas/AddressInput'
                shippingAddress:
                  $ref: '#/components/schemas/ShippingAddressInput'
      responses:
        '200':
          description: Updated cart
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cart'

  /cart/{cartId}/shipping:
    patch:
      tags: [Cart]
      summary: Set the selected shipping option
      description: >
        Shipping options are fetched from GET /cart/{cartId}/shipping-options.
        The frontend selects one and calls this endpoint to persist the choice.
      parameters:
        - name: cartId
          in: path
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/Locale'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [deliveryDate]
              properties:
                deliveryDate:
                  type: string
                  format: date
                  description: Customer-chosen delivery date. Uniquely identifies the shipping option — the server resolves method, carrier, cost, and predefinedDeliveryDate from this value.
      responses:
        '200':
          description: Updated cart
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cart'

  /cart/{cartId}/discount-code:
    patch:
      tags: [Cart]
      summary: Apply or remove a discount code
      description: >
        The backend validates the code and returns the updated cart with pricing recalculated.
        Send `code: null` to remove the current discount.
      parameters:
        - name: cartId
          in: path
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/Locale'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code:
                  type: [string, 'null']
                  description: Discount code to apply, or null to remove
      responses:
        '200':
          description: Updated cart
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cart'
        '400':
          description: Invalid, expired, or ineligible discount code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /cart/{cartId}/shipping-options:
    get:
      tags: [Cart]
      summary: Get available shipping options for a cart
      parameters:
        - name: cartId
          in: path
          required: true
          schema:
            type: string
        - name: countryCode
          in: query
          required: true
          schema:
            type: string
            minLength: 2
            maxLength: 2
          description: ISO 3166-1 alpha-2 destination country code
        - $ref: '#/components/parameters/Locale'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShippingOptions'
        '404':
          description: Cart not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /cart/{cartId}/checkout:
    post:
      tags: [Cart]
      summary: Create an order from a cart
      description: >
        The cart must have billing/shipping addresses and a shipping option set.
        VAT rate (derived from shipping address), shipping cost, product availability,
        and discount code decrements are all resolved server-side.
      parameters:
        - name: cartId
          in: path
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/Locale'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [paymentMethod]
              properties:
                poNumber:
                  type: string
                  description: Customer purchase order number
                orderNotes:
                  type: string
                paymentMethod:
                  type: string
                  enum: [bank, card, klarna]
      responses:
        '200':
          description: Order created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  orderNumber:
                    type: string
                  fulfillmentStatus:
                    type: string
                    enum: [PENDING]
                  paymentMethod:
                    type: string
                    enum: [bank, card, klarna]
                  createdAt:
                    type: string
                    format: date-time
        '400':
          description: Cart missing required data (addresses or shipping option)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '409':
          description: A product in the cart is no longer available in the requested quantities
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  # ─── Translations ─────────────────────────────────────────────────────────

  /translations:
    get:
      tags: [Translations]
      summary: Get translation bundle for a locale
      description: >
        Returns the full UI string bundle for the resolved locale. Storyblok datasource
        entries are flat key-value pairs — the API parses dot-notation keys into a nested
        object before returning (e.g. "checkout.submit" becomes { checkout: { submit: "..." } }).
        String values are plain text and ICU-compatible.
        Locale resolution follows the standard convention: `?locale=` query param, falling
        back to `Accept-Language` header, falling back to `en`. Unsupported locales silently
        fall back to `en`.
        Cache: 60 minutes, keyed on `locale`.
        Note: Storyblok webhook invalidation should be added when cache TTL becomes a pain
        point — low effort, high value for translators.
      parameters:
        - $ref: '#/components/parameters/Locale'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TranslationBundle'

  # ─── Commerce ─────────────────────────────────────────────────────────────

  /vat/{vatNumber}:
    get:
      tags: [Commerce]
      summary: Validate a VAT number
      description: Validates against external providers (VIES and/or vatLayer).
      parameters:
        - name: vatNumber
          in: path
          required: true
          schema:
            type: string
          description: 'VAT number to validate, e.g. GB123456789'
        - $ref: '#/components/parameters/Locale'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VatValidationResponse'
        '400':
          description: Missing or malformed VAT number
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: VAT number not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Provider error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  # ─── Countries ───────────────────────────────────────────────────────────────

  /countries:
    get:
      tags: [Countries]
      summary: List supported countries
      description: >
        Returns all countries supported by the platform, sorted alphabetically by localized name.
        Intended for populating country selects in billing and shipping address forms.
        Cache: 24 hours. `locale` is part of the cache key.
      parameters:
        - $ref: '#/components/parameters/Locale'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Country'

# ─── Components ─────────────────────────────────────────────────────────────

components:

  parameters:

    Locale:
      name: locale
      in: query
      required: false
      schema:
        type: string
        default: en
        example: pt
      description: >
        BCP 47 locale tag (e.g. `en`, `pt`, `fr`). Defaults to `en` when absent or unrecognized.
        Uses `Accept-Language` header as fallback when not provided.
        Untranslated content falls back silently to `en`.
        This parameter is part of the cache key on cacheable endpoints.

  schemas:

    # ── Translations ────────────────────────────────────────────────────────

    TranslationBundle:
      type: object
      description: >
        Nested map of UI translation strings for a given locale. Built by parsing
        dot-notation Storyblok datasource keys into a nested object structure.
        Values are ICU-compatible strings — plain text today, but may include
        interpolation variables (e.g. {count}) or plural forms in the future.
      additionalProperties:
        oneOf:
          - type: string
            description: Leaf translation string (ICU-compatible)
          - type: object
            description: Nested group of translation strings
            additionalProperties: {}
      example:
        checkout:
          submit: "Place order"
          back: "Back"
        cart:
          empty: "Your cart is empty"

    # ── Catalog ─────────────────────────────────────────────────────────────

    BaseProduct:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        slug:
          type: string
        sku:
          type: string
        category:
          type: object
          properties:
            id:
              type: string
            title:
              type: string
            productType:
              type: string
              enum: [garments, caps, accessories]
        composition:
          type: string
          example: 100% Cotton
        fabricWeight:
          type: string
          example: 180gsm
        moqQty:
          type: integer
        lowMoqQty:
          type: [integer, 'null']
        productImage:
          type: string
          description: Ghost/flat shot — white background, no model
        lifestyleImage:
          type: string
        marketingImages:
          type: array
          items:
            type: string
        description:
          type: string
        configuration:
          type: object
          properties:
            customizations:
              type: object
              properties:
                dye:
                  type: object
                  properties:
                    title:
                      type: string
                      description: Display name — e.g. "Color" or "Cap Color"
                    techniques:
                      type: array
                      items:
                        type: object
                        properties:
                          id:
                            type: string
                          title:
                            type: string
                          slug:
                            type: string
                          dyeCategories:
                            type: array
                            items:
                              $ref: '#/components/schemas/DyeCategoryBase'
                print:
                  type: object
                  properties:
                    sides:
                      type: array
                      items:
                        type: string
                        enum: [front, back, left, right]
                    techniques:
                      type: array
                      items:
                        $ref: '#/components/schemas/PrintTechnique'
                    placements:
                      type: [object, 'null']
                      properties:
                        front:
                          type: [array, 'null']
                          items:
                            $ref: '#/components/schemas/PrintPlacement'
                        back:
                          type: [array, 'null']
                          items:
                            $ref: '#/components/schemas/PrintPlacement'
                label:
                  type: object
                  properties:
                    title:
                      type: string
                      description: Display name — e.g. "Neck Label" or "Cap Label"
                    sizes:
                      type: array
                      items:
                        $ref: '#/components/schemas/LabelSize'

    PrintTechnique:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        description:
          type: string
        thumbnail:
          type: string
        type:
          type: string
          enum: [screenPrint, dtf, dtg, hybridDtg, embroidery, heatTransfer]
        requiresFoilColor:
          type: boolean
          description: true only for heatTransfer — show foil color selector in UI

    PrintPlacement:
      type: object
      properties:
        title:
          type: string
        width:
          type: number
        height:
          type: number
        fromCenter:
          type: number
          description: Horizontal offset from center of print area (negative = left)
        fromYOrigin:
          type: number
          description: Vertical offset from neck seam

    LabelSize:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        slug:
          type: string
        thumbnail:
          type: string
        sampleArtworkUrl:
          type: [string, 'null']
          description: Original sample file — for demo/testing flow
        sampleArtworkConvertedUrl:
          type: [string, 'null']
          description: Converted sample file — for demo/testing flow

    WovenLabelOptions:
      type: object
      properties:
        placements:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              title:
                type: string
              stitchingOptions:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                    title:
                      type: string
        sizes:
          type: array
          items:
            $ref: '#/components/schemas/LabelSize'

    DyeCategoryBase:
      type: object
      description: Dye category without the colors list — embedded in BaseProduct dye techniques
      properties:
        id:
          type: string
        title:
          type: string
        description:
          type: [string, 'null']
        slug:
          type: string
        defaultDyeId:
          type: string
          description: ID of the default pre-selected color in this category

    DyeCategory:
      allOf:
        - $ref: '#/components/schemas/DyeCategoryBase'
        - type: object
          properties:
            dyes:
              type: array
              items:
                type: object
                properties:
                  id:
                    type: string
                  title:
                    type: string
                  hexCode:
                    type: string

    # ── Configuration ───────────────────────────────────────────────────────

    Configuration:
      type: object
      properties:
        id:
          type: string
        deliverySpeed:
          type: string
          enum: [flexible, fast]
        mockups:
          type: object
          properties:
            front:
              type: [string, 'null']
            back:
              type: [string, 'null']
            left:
              type: [string, 'null']
            right:
              type: [string, 'null']
            label:
              type: [string, 'null']
        defaultMockup:
          type: [string, 'null']
          description: Always mirrors mockups.front when set
        baseProduct:
          $ref: '#/components/schemas/BaseProduct'
        dye:
          type: [object, 'null']
          properties:
            technique:
              type: object
              properties:
                id:
                  type: string
                title:
                  type: string
                slug:
                  type: string
            color:
              type: object
              properties:
                id:
                  type: string
                title:
                  type: string
                hexCode:
                  type: string
                pantoneRef:
                  type: [string, 'null']
        print:
          type: [object, 'null']
          properties:
            minProductSize:
              type: [string, 'null']
              description: Minimum garment/product size selected by user — e.g. "L"
            front:
              $ref: '#/components/schemas/PrintSide'
            back:
              $ref: '#/components/schemas/PrintSide'
            left:
              $ref: '#/components/schemas/PrintSide'
            right:
              $ref: '#/components/schemas/PrintSide'
        label:
          type: [object, 'null']
          properties:
            size:
              type: [object, 'null']
              properties:
                id:
                  type: string
                title:
                  type: string
            placement:
              type: [object, 'null']
              properties:
                id:
                  type: string
                title:
                  type: string
            stitching:
              type: [object, 'null']
              properties:
                id:
                  type: string
                title:
                  type: string
            artworkOriginalUrl:
              type: [string, 'null']
            artworkConvertedUrl:
              type: [string, 'null']
            artworkConvertedPngUrl:
              type: [string, 'null']
            colorCount:
              type: [integer, 'null']

    ConfigurationUpdate:
      type: object
      description: All fields optional — only send what needs updating
      properties:
        deliverySpeed:
          type: string
          enum: [flexible, fast]
        mockups:
          type: object
          properties:
            front:
              type: [string, 'null']
            back:
              type: [string, 'null']
            left:
              type: [string, 'null']
            right:
              type: [string, 'null']
            label:
              type: [string, 'null']
        defaultMockup:
          type: [string, 'null']
        dye:
          type: [object, 'null']
          properties:
            techniqueId:
              type: string
            colorId:
              type: string
        print:
          type: [object, 'null']
          properties:
            minProductSize:
              type: [string, 'null']
            front:
              $ref: '#/components/schemas/PrintSideInput'
            back:
              $ref: '#/components/schemas/PrintSideInput'
            left:
              $ref: '#/components/schemas/PrintSideInput'
            right:
              $ref: '#/components/schemas/PrintSideInput'
        label:
          type: [object, 'null']
          properties:
            sizeId:
              type: [string, 'null']
            placementId:
              type: [string, 'null']
            stitchingId:
              type: [string, 'null']
            artworkOriginalUrl:
              type: [string, 'null']
            artworkConvertedUrl:
              type: [string, 'null']
            artworkConvertedPngUrl:
              type: [string, 'null']
            colorCount:
              type: [integer, 'null']

    # ── Shared Types ────────────────────────────────────────────────────────

    Address:
      type: object
      required: [firstName, lastName, email, addressLine1, zip, city, country, countryCode, phone]
      properties:
        firstName:
          type: string
        lastName:
          type: string
        email:
          type: string
          format: email
        company:
          type: [string, 'null']
        vatNumber:
          type: string
          description: Billing address only — not applicable for shipping address
        addressLine1:
          type: string
        addressLine2:
          type: [string, 'null']
        zip:
          type: string
        city:
          type: string
        state:
          type: [string, 'null']
          description: Optional — not required for countries without states
        country:
          type: string
        countryCode:
          type: string
          description: ISO 3166-1 alpha-2
          minLength: 2
          maxLength: 2
        phone:
          type: string

    AddressInput:
      type: object
      description: Partial billing address — all fields optional for progressive save
      properties:
        firstName:
          type: string
        lastName:
          type: string
        email:
          type: string
          format: email
        company:
          type: [string, 'null']
        vatNumber:
          type: [string, 'null']
        addressLine1:
          type: string
        addressLine2:
          type: [string, 'null']
        zip:
          type: string
        city:
          type: string
        state:
          type: [string, 'null']
        country:
          type: string
        countryCode:
          type: string
          minLength: 2
          maxLength: 2
        phone:
          type: string

    ShippingAddressInput:
      type: object
      description: Partial shipping address — vatNumber not applicable here
      properties:
        firstName:
          type: string
        lastName:
          type: string
        email:
          type: string
          format: email
        company:
          type: [string, 'null']
        addressLine1:
          type: string
        addressLine2:
          type: [string, 'null']
        zip:
          type: string
        city:
          type: string
        state:
          type: [string, 'null']
        country:
          type: string
        countryCode:
          type: string
          minLength: 2
          maxLength: 2
        phone:
          type: string

    PrintSide:
      type: object
      description: Represents a single configured print side on a saved configuration (response shape)
      properties:
        technique:
          type: [object, 'null']
          properties:
            id:
              type: string
            title:
              type: string
            type:
              type: string
              enum: [screenPrint, dtf, dtg, hybridDtg, embroidery, heatTransfer]
        fromCenter:
          type: number
          description: Horizontal distance from center — positive = right, negative = left
        fromYOrigin:
          type: number
          description: Vertical distance from y-axis origin (neck seam)
        artworkWidth:
          type: number
        artworkHeight:
          type: number
        artworkRatio:
          type: number
        artworkOriginalUrl:
          type: [string, 'null']
        artworkConvertedUrl:
          type: [string, 'null']
        foilColor:
          description: Only applicable when technique.type is heatTransfer
          type: [object, 'null']
          properties:
            id:
              type: string
            colour:
              type: string
            hexCode:
              type: string
            pantoneCode:
              type: string

    PrintSideInput:
      type: object
      description: Input shape for writing a print side via PATCH /configuration/{id}
      properties:
        techniqueId:
          type: [string, 'null']
        fromCenter:
          type: number
        fromYOrigin:
          type: number
        artworkWidth:
          type: number
        artworkHeight:
          type: number
        artworkRatio:
          type: number
        artworkOriginalUrl:
          type: [string, 'null']
        artworkConvertedUrl:
          type: [string, 'null']
        foilColorId:
          type: [string, 'null']
          description: Only applicable when technique is heatTransfer

    # ── Calculator ──────────────────────────────────────────────────────────

    CalculatorPrintSideInput:
      type: object
      properties:
        techniqueId:
          type: string
        artwork:
          type: object
          properties:
            colors:
              type: integer
              minimum: 1
            size:
              type: object
              description: Defaults to 5x5 if omitted
              properties:
                width:
                  type: number
                height:
                  type: number

    CalculatorResponse:
      type: object
      properties:
        data:
          type: object
          description: Echo of the input params used for this calculation
          properties:
            baseProductId:
              type: string
            color:
              type: object
              properties:
                technique:
                  type: string
                category:
                  type: string
                dye:
                  type: string
            quantity:
              type: integer
            print:
              type: object
              additionalProperties:
                $ref: '#/components/schemas/CalculatorPrintSideInput'
            wovenLabel:
              type: object
              properties:
                size:
                  type: string
        result:
          type: object
          properties:
            leadTime:
              type: integer
              description: Production days
            deliveryDate:
              type: string
              format: date
              description: Estimated delivery date (ISO date string)
            price:
              type: number
              description: Cost per item (EUR)
            developmentCosts:
              type: object
              properties:
                artwork:
                  type: number
                screen:
                  type: number
                technique:
                  type: number
                service:
                  type: number
            developmentCostsTotal:
              type: number
            subtotal:
              type: number
              description: (price × quantity) + developmentCostsTotal, rounded to 2dp

    Error:
      type: object
      required: [code, message]
      description: >
        Standard error shape. Use `code` for programmatic error handling.
        `message` is informational only — do not parse or display it to users.
      properties:
        code:
          type: string
          description: Machine-readable error identifier in UPPER_SNAKE_CASE (e.g. CART_NOT_FOUND)
        message:
          type: string
          description: Human-readable debugging hint — not part of the stable contract

    ValidationError:
      type: object
      required: [code, message, errors]
      description: Returned on 400 when request fails field-level validation.
      properties:
        code:
          type: string
          enum: [VALIDATION_ERROR]
        message:
          type: string
        errors:
          type: array
          items:
            type: object
            required: [field, code]
            properties:
              field:
                type: string
                description: Dot-notation field path — e.g. "billingAddress.email"
              code:
                type: string
                description: UPPER_SNAKE_CASE error code — e.g. REQUIRED, INVALID_FORMAT

    Pagination:
      type: object
      required: [page, limit, total, totalPages]
      properties:
        page:
          type: integer
          minimum: 1
          description: Current page (1-indexed)
        limit:
          type: integer
          description: Items per page
        total:
          type: integer
          description: Total items across all pages
        totalPages:
          type: integer
          description: ceil(total / limit) — computed server-side

    # ── Upload ──────────────────────────────────────────────────────────────

    UploadEvent:
      description: One of four possible streaming NDJSON events
      oneOf:
        - description: Upload started
          type: object
          properties:
            event:
              type: string
              enum: [start]
            progress:
              type: number
            step:
              type: object
              properties:
                current:
                  type: integer
                total:
                  type: integer
        - description: Progress update
          type: object
          properties:
            event:
              type: string
              enum: [progress]
            progress:
              type: number
            step:
              type: object
              properties:
                current:
                  type: integer
                total:
                  type: integer
        - description: Upload complete
          type: object
          properties:
            event:
              type: string
              enum: [result]
            progress:
              type: number
              enum: [100]
            originalUrl:
              type: string
            convertedUrl:
              type: [string, 'null']
              description: null when convert was not requested or produced no output
        - description: Upload failed
          type: object
          properties:
            event:
              type: string
              enum: [error]
            progress:
              type: number
            message:
              type: string

    # ── Cart ────────────────────────────────────────────────────────────────

    Cart:
      type: object
      properties:
        id:
          type: string
        discountCode:
          type: [object, 'null']
          properties:
            id:
              type: string
            type:
              type: string
              enum: [fixed, percentage]
            code:
              type: string
            amount:
              type: number
            scope:
              type: string
              enum: [products-only, excludes-shipping, order-total]
        shippingOption:
          type: [object, 'null']
          properties:
            method:
              type: string
              enum: [standard, express]
            cost:
              type: number
            deliveryDate:
              type: string
              format: date
              description: Customer-chosen delivery date
            carrier:
              type: string
              enum: [ups, fedex]
            serviceName:
              type: string
              description: Human-readable service name, e.g. "UPS Express Saver"
        billingAddress:
          oneOf:
            - $ref: '#/components/schemas/Address'
            - type: 'null'
        shippingAddress:
          oneOf:
            - $ref: '#/components/schemas/Address'
            - type: 'null'
        lineItems:
          type: array
          items:
            $ref: '#/components/schemas/CartLineItem'

    CartLineItem:
      type: object
      properties:
        id:
          type: string
        configuration:
          $ref: '#/components/schemas/Configuration'
        sizeAllocations:
          type: array
          items:
            type: object
            properties:
              size:
                type: string
                example: L
              quantity:
                type: integer
        isReorder:
          type: boolean
          description: true when this line item originated from a previous order
        quantity:
          type: integer
        unitPrice:
          type: number
        productSubtotal:
          type: number
        artworkFee:
          type: number
        screenFee:
          type: number
        techniqueFee:
          type: number
        serviceFee:
          type: number
        developmentCostsSubtotal:
          type: number
        subtotal:
          type: number
        discountAbsoluteValue:
          type: number
        subtotalAfterDiscount:
          type: number
        vatCost:
          type: number
        lineTotal:
          type: number

    ShippingOptions:
      type: object
      properties:
        dates:
          type: array
          items:
            type: object
            properties:
              deliveryDate:
                type: string
                format: date
                description: Estimated delivery date
              shippingCost:
                type: number
                description: EUR
              service:
                type: object
                properties:
                  type:
                    type: string
                    enum: [standard, express]
                  name:
                    type: string
                  carrier:
                    type: string
                    enum: [ups, fedex]
                  transitTime:
                    type: object
                    properties:
                      days:
                        type: integer
                      bufferDays:
                        type: integer
              category:
                type: string
                enum: [fast, standard, long-term]
        manualQuoteRequired:
          type: boolean
          description: true when automated pricing is unavailable
        error:
          type: string

    # ── Countries ───────────────────────────────────────────────────────────

    Country:
      type: object
      required: [code, name]
      properties:
        code:
          type: string
          description: ISO 3166-1 alpha-2 country code, e.g. "DE"
          minLength: 2
          maxLength: 2
          example: DE
        name:
          type: string
          description: Localized country name, e.g. "Germany"
          example: Germany

    # ── Commerce ────────────────────────────────────────────────────────────

    VatValidationResponse:
      type: object
      properties:
        valid:
          type: boolean
        formatIsValid:
          type: boolean
        vatNumber:
          type: string
        countryCode:
          type: string
        companyName:
          type: string
        address:
          type: string
        source:
          type: string
          enum: [vies, vat-layer]
        unavailable:
          type: boolean
          description: true when the validation service is temporarily unavailable
        error:
          type: string
